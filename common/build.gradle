jar {
    enabled = true
    version = '0.0.1'
}

dependencies {

}

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId 'dive'
            artifactId 'http-common'
            version = '0.0.1'
            from components.java
        }
    }
    repositories {
        maven {
            url = "$buildDir/repo"
        }
    }
}
